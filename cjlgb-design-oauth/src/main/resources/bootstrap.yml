spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.63.128:8001
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        shared-configs:
          - application.yaml
          - application-oauth.yaml